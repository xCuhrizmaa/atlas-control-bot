import os
from github import Github, GithubException

GITHUB_TOKEN = os.getenv("GITHUB_TOKEN")

def next_version(repo):

```
try:
    commits = repo.get_commits()
    return f"v{commits.totalCount + 1}"
except:
    return "v1"
```

def generate_project_files(project_type):

```
files = {}

files["backend/main.py"] = """from fastapi import FastAPI
```

app = FastAPI()

@app.get("/")
def root():
return {"message": "Atlas backend running"}
"""

```
files["backend/requirements.txt"] = "fastapi\nuvicorn"

files["README.md"] = f"# atlas-{project_type}\nGenerated by Atlas AI"

return files
```

def create_or_update_repo(project_type):

```
g = Github(GITHUB_TOKEN)
user = g.get_user()

repo_name = f"atlas-{project_type}"

try:
    repo = user.get_repo(repo_name)
except GithubException:
    repo = user.create_repo(
        name=repo_name,
        description=f"AI generated project: {project_type}",
        private=False,
        auto_init=True
    )

version = next_version(repo)

files = generate_project_files(project_type)

for filename, content in files.items():

    try:
        existing = repo.get_contents(filename)

        repo.update_file(
            filename,
            f"{version} Atlas update",
            content,
            existing.sha
        )

    except GithubException:

        repo.create_file(
            filename,
            f"{version} Atlas initial build",
            content
        )

return repo_name, version
```
